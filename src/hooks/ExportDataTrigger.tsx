'use client';

const ExportDataTrigger = () => {
  return <div className="donneesExportees" style={{ position: 'absolute', width: 0, height: 0, overflow: 'hidden' }} />;
};

export default ExportDataTrigger;
